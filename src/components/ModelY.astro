---
import Section from "./Section.astro"
---

<Section
  title="Model Y"
  subtitle="Concertar una prueba de conducción"
  color="white"
  id="model-y"
>
  <div slot="footer" class="gap-x-6 flex flex-wrap justify-center gap-3 mx-auto">
    <a
      class="border-[3px] border-gray-600 bg-gray-600 text-sm rounded font-medium text-white px-12 py-2 inline-block transition-colors hover:border-gray-500 hover:bg-gray-500"
      href="#"
    >
      Explorar inventario</a
    >
    <a
      class="bg-white text-sm rounded font-medium px-12 py-2 transition-colors hover:bg-black hover:text-white inline-flex justify-center items-center text-black"
      href="#"
    >
      Pedido personalizado</a
    >
  </div>

  <video
    slot="background"
    id="video-background"
    class="object-center object-cover h-full w-full"
    autoplay
    muted
    loop
   >
	</video>
</Section>

  <script>
    const videoElement = document.querySelector('#video-background');

    const updateVideoSrc = () => {
      const isMobile = window.innerWidth < 640;
      const videoSrc = isMobile ? '/modelYmobile.mp4' : '/Model-Y.mp4';
      videoElement.setAttribute('src', videoSrc);
    };

  window.addEventListener('resize', updateVideoSrc);

  updateVideoSrc();
</script>